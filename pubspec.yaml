name: dumsor_tracker
description: A Flutter app to track power outages in Ghana.

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.6.0
  # Add other packages here (like firebase_core, cloud_firestore) later

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
